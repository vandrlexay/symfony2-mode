### Symfony2 mode for emacs ###

Now only tow sthings supported:

  - PSR coding standard by default
  - jumping to current action's template. To go to remplate, while inside action body, press `A-s t`.
